import React from "react";
// import useFetch from "../useFetch";

export default function Home() {
  // const {
  //   data: users,
  //   isPending,
  //   error,
  // } = useFetch("http://localhost:3000/users");
  return (
    <>
      {/* <section>
        {error && <p>{error}</p>}
        {isPending && <p>Loading users...</p>}
        {users && console.log(await users["heba"])}
      </section> */}
    </>
  );
}
